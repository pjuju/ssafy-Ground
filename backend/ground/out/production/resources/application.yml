spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ground?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        show_sql: true
      format_sql: true

      
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
    

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace
